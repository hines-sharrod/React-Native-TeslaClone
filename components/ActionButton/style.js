import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  buttonContainer: {
    width: '100%',
    padding: 25,
    alignItems: 'center'
  },
  actionButton: {
    borderRadius: 25,
    width: '100%',
    padding: 20,
    bottom: 200,
    backgroundColor: '#000',
    alignItems: 'center'
  },
  buttonText: {
    color: '#fff',
    fontSize: 16
  }
});

export default styles;